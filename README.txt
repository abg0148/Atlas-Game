1. enter the name of country or capital to startwith
2. next enter the limit on the number of names you want to print
   -if "no element found" comes out. you are out of luck. as no further order of names can be created!!
3. demo1 will print along with the names whether they are country or capitals
4. demo2 will not print whether the name is of country or capital.
5. deom3 will show how big length list can be generated from each alphabet using the applied algorithm